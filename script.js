const header=document.getElementById('siteHeader');function onScroll(){if(window.scrollY>70){header.classList.add('is-clear')}else{header.classList.remove('is-clear')}}window.addEventListener('scroll',onScroll,{passive:true});onScroll();document.addEventListener('click',e=>{if(!e.target.closest('.site-header')&&document.body.classList.contains('menu-open'))document.body.classList.remove('menu-open');});